import React from 'react';
import TransferList from './TransferList';

const StoreToStore = () => {
  return <TransferList fromType="store" toType="store" title="Store to Store Transfers" />;
};

export default StoreToStore;
